# .env.example
# Your shared Claude (Anthropic) API key, used server-side by the local dev
# server (server.ts) so you don't have to paste a key into the app itself.
ANTHROPIC_API_KEY=
WEBSITE_API_KEY=
DATA_BOARD_KEY=
