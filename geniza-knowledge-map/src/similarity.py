"""Stage 4 (milestone 2, spec §13, optional): nearest-neighbor computation
over embeddings produced by embeddings.py. Not implemented — see
embeddings.py docstring."""
from __future__ import annotations


def nearest_neighbors(doc_id: str, embedding_matrix, doc_ids: list[str], top_k: int = 10):
    raise NotImplementedError("Milestone 2 — see embeddings.py.")
